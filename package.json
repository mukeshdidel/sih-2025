{
  "name": "sih",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "docker:start": "bun run db:migrate && bun run db:seed && bun run build && bun run web:start",
    "db:migrate": "cd packages/db && bunx prisma migrate deploy",
    "db:seed": "bun run packages/db/src/truncate.ts &&  bun run packages/db/src/coreSeed.ts && bun run packages/db/src/foodseed1.ts && bun run packages/db/src/cuisine.ts && bun run packages/db/src/rec.ts",
    "db:generate": "cd packages/db && bunx prisma generate",
    "web:start": "cd apps/web && bun run start"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.20",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "pipeline": {
    "...": "..."
  },
  "globalEnv": [
    "DATABASE_URL",
    "JWT_SECRET"
  ]
}